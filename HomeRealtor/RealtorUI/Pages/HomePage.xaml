<Page x:Class="RealtorUI.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RealtorUI.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage">
    <Page.Resources>
        <Style x:Key="LargeTileStyle" TargetType="Controls:Tile">
            <Setter Property="Width" Value="200" />
            <Setter Property="Height" Value="125" />
            <Setter Property="TitleFontSize" Value="10" />
        </Style>

        <Style x:Key="SmallTileStyle" TargetType="Controls:Tile">
            <Setter Property="Width" Value="125" />
            <Setter Property="Height" Value="125" />
            <Setter Property="TitleFontSize" Value="10" />
        </Style>
    </Page.Resources>
    <Grid>
        <WrapPanel Margin="50" Grid.Row="1" Grid.Column="1" Width="700" Height="300" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Controls:Tile Title="Mail" Style="{StaticResource ResourceKey=LargeTileStyle}" Content="ImageHere" Background="Teal" Margin="3"/>
            <Controls:Tile Title="Desktop" Style="{StaticResource ResourceKey=LargeTileStyle}"  Margin="3">
                <!--<Controls:Tile.Background>
                    <ImageBrush ImageSource="Images/windesktop.jpg" />
                </Controls:Tile.Background>-->
            </Controls:Tile>
            <Controls:Tile Title="Finance" Style="{StaticResource ResourceKey=LargeTileStyle}" Background="Green" Width="255" />
            <Controls:Tile Title="People" Style="{StaticResource ResourceKey=LargeTileStyle}" Background="#D2691E" />
            <Controls:Tile Title="Weather" Style="{StaticResource ResourceKey=LargeTileStyle}" Background="#1E90FF" />
            <Controls:Tile Title="Weather" Style="{StaticResource ResourceKey=SmallTileStyle}" Background="#1E90FF" />
            <Controls:Tile Title="Store" Style="{StaticResource ResourceKey=SmallTileStyle}" Background="Green" />
        </WrapPanel>
    </Grid>
</Page>
